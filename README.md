A job processing service is a backend system that accepts tasks, stores them, relibly and completes them in the background while client track progress

1. API - How clients submit work

2. PostgreSQL - Persistent job store

3. Queue - Decides what work should be processed next

Tech stack includes: TypeScript, Node.js, Express, PostgreSQL, Prisma (DB Access), Redis, Docker Compose
